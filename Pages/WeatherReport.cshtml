@page
@model Copernicus_Weather.Pages.WeatherReportModel
@{
    ViewData["Title"] = "WeatherReport";
}

<h1>Weather report</h1>

<iframe src='https://mars.nasa.gov/layout/embed/image/insightweather/' width='100%' height='700' scrolling='no'
    frameborder='0'></iframe>

@* <table class="table table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Days[0].DayOfTheYear)</th>
            <th>Average @Html.DisplayNameFor(m => m.Days[0].AtmosphericTemperature)</th>
            <th>Average @Html.DisplayNameFor(m => m.Days[0].HorizontalWindSpeed)</th>
            <th>@Html.DisplayNameFor(m => m.Days[0].Season)</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var sol in Model.Days)
        {
        <tr>
            <td>
                Sol @(sol.DayOfTheYear)
            </td>
            <td>
                @(sol.AtmosphericTemperature.Average)
            </td>
            <td>
                @(sol.HorizontalWindSpeed.Average)
            </td>
            <td>
                @(sol.Season)
            </td>
        </tr>
        }
    </tbody>
</table> *@
